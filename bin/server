#!/usr/bin/env node

rest = require('../lib/middleware.js');
connect = require('connect');

server = connect.createServer();
server.use('/hookio', rest());
server.use(connect.static('public'));
server.listen(3000);
